import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Sales Admin",
};

const Sales = () => {
  return (
    <>
      <div>Sales</div>
    </>
  );
};

export default Sales;
